name: Slider
icon: images
preview: fields

tabs:
  content:
    label: Content
    fields:
      images:
        type: structure
        label: Images
        fields:
          image:
            type: files
            query: page.files
            accept: image
            multiple: false
          alt:
            type: text
            label: Alt text
        columns:
          image:
            type: files
            width: 1/3
          alt:
            width: 2/3

  layout:
    extends: tabs/layout
